## 第十八章 多元函数的极限与连续 参考题提示

## 第一组参考题 (163 页)

1. 用三分法证明任意 \( {x}_{0} \in  \left\lbrack  {a, b}\right\rbrack \) 的某个小邻域上收敛是一致的,再用有限覆盖定理.

2. \( \mathop{\max }\limits_{{a \leq  y \leq  \xi }}\{ f\left( {\xi , y}\right) \}  = \mathop{\max }\limits_{{0 \leq  k \leq  1}}\{ f\left( {\xi , a + k\left( {\xi  - a}\right) }\right) \} \) .

3. 证明有界闭.

4. 利用齐次线性方程组有非零解的条件. 先考虑 \( \mathop{\sum }\limits_{{j = 1}}^{n}{c}_{j}{\int }_{0}^{1}{f}_{i}\left( x\right) {f}_{j}\left( x\right) \mathrm{d}x = 0\left( {i = 1,2,\cdots , n}\right) \) 的非零解,再对 \( F\left( x\right)  = \mathop{\sum }\limits_{{i = 1}}^{n}{c}_{i}{f}_{i}\left( x\right) \) 讨论.

5. 设 \( B \) 以 \( \mathbf{a} \) 为圆心,以 \( r \) 为半径,最小覆盖圆盘应满足 \( {r}^{2} = \mathop{\max }\limits_{{1 \leq  i \leq  k}}\left\{  {{d}^{2}\left( {\mathbf{a},{\mathbf{p}}_{i}}\right) }\right\} \) ,证明 \( r \) 作为 \( \mathbf{a} \) 的连续函数能取到最小值.

6. (1) 考虑 \( G\left( \mathbf{x}\right) \) 在 \( {S}^{1} \) 上的最大值; (2) 利用 \( \mathbf{B} \) 是正定矩阵的充分必要条件是 \( \mathbf{B} = {\mathbf{L}}^{\mathrm{T}}\mathbf{L} \) , 其中 \( \mathbf{L} \) 非奇异,并证明对任意实对称矩阵 \( \mathbf{C} \) ,有

\[
{\lambda }_{\min } \leq  \frac{{\mathbf{y}}^{\mathrm{T}}\mathbf{{Cy}}}{{\mathbf{y}}^{\mathrm{T}}\mathbf{y}} \leq  {\lambda }_{\max },\;\mathbf{y} \in  E,
\]

\( {\lambda }_{\min }\left( {\lambda }_{\max }\right) \) 是 \( \mathbf{C} \) 的最小 (最大) 特征值.

## 第二组参考题 (164 页)

1. 先证明 \( {\mathcal{T}}^{{n}_{0}} \) 有惟一不动点,再证明此不动点就是 \( \mathcal{T} \) 的惟一不动点.

2. 令 \( g\left( \mathbf{x}\right)  = \left| {f\left( \mathbf{x}\right)  - \mathbf{x}}\right| \) ,证明 \( g \) 是 \( \Omega \) 上的连续函数,并且在 \( \Omega \) 上取到零点.

3. 用反证法.

4. 根据定义.

5. 用上题.

6. 用上题.

7. (1) 根据定义; (2) 利用有界闭区间上连续函数性质.

8. (1) 根据定义; (2) 作实轴到区间 \( \left( {-1,1}\right) \) 上的同胚 \( h \) ,再对复合映射 \( h \circ  f \) 讨论.

9. R 上定义在有界开集上的无界连续函数不可扩张到全空间. 先利用 Cauchy 准则把一致连续函数扩张到开集的闭包上, 再用 Tietze 扩张定理.

10. 利用有理点集在 \( {\mathbf{R}}^{n} \) 中的稠密性及 Cauchy 准则.
