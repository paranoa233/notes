## 3. 对习题课的建议

(1)关于求极限. 极坐标变换是讨论二元函数极限的有效手段, 学生们很喜欢用, 但在使用时必须小心. 例如

\[
f\left( {x, y}\right)  = \frac{{x}^{2}y}{{x}^{4} + {y}^{2}},\;\left( {x, y}\right)  \neq  \left( {0,0}\right)
\]

的极限 \( \mathop{\lim }\limits_{{\left( {x, y}\right)  \rightarrow  \left( {0,0}\right) }}f\left( {x, y}\right) \) 不存在,但如果用极坐标变换,则

\[
f\left( {x, y}\right)  = \frac{r{\cos }^{2}\theta \sin \theta }{{r}^{2}{\cos }^{4}\theta  + {\sin }^{2}\theta } \rightarrow  0,\;\text{ 如果 }\theta  \neq  0,\pi ;
\]

而当 \( \theta  = 0,\pi \) 时, \( y = 0 \) ,故 \( f\left( {x, y}\right)  = 0 \) ,因此容易误认为 \( \mathop{\lim }\limits_{{\left( {x, y}\right)  \rightarrow  \left( {0,0}\right) }}f\left( {x, y}\right)  = 0 \) . 产生错误的原因在于忽视了用极坐标变换计算二元函数极限时, 要求极限过程对 \( \theta  \in  \lbrack 0,{2\pi }) \) 一致地成立. 而上述计算中极限过程与 \( \theta \) 有关. 事实上,如果我们选择一条特殊的趋于原点的曲线

\[
L = \left\{  {\left( {r,\theta }\right)  \mid  \sin \theta  = r{\cos }^{2}\theta }\right\}  ,
\]

则

\[
{\left. f\left( x, y\right) \right| }_{L} = {\left. \frac{r{\cos }^{2}\theta \sin \theta }{{r}^{2}{\cos }^{4}\theta  + {\sin }^{2}\theta }\right| }_{L} = \frac{1}{2},
\]

从而二元函数极限不存在.

(2) 关于连续性. 在证明函数 \( f\left( {x, y}\right) \) 在点 \( \left( {{x}_{0},{y}_{0}}\right) \) 连续时,如果需要的话,学生们都会将 \( f\left( {x, y}\right)  - f\left( {{x}_{0},{y}_{0}}\right) \) 写为

\[
f\left( {x, y}\right)  - f\left( {{x}_{0},{y}_{0}}\right)  = \left\lbrack  {f\left( {x, y}\right)  - f\left( {{x}_{0}, y}\right) }\right\rbrack   + \left\lbrack  {f\left( {{x}_{0}, y}\right)  - f\left( {{x}_{0},{y}_{0}}\right) }\right\rbrack  .
\]

但经常会犯的错误是,由 \( f \) 对 \( x \) 的连续性得出 \( \forall \varepsilon  > 0,\exists \delta  > 0 \) ,当 \( \left| {x - {x}_{0}}\right|  < \delta \) 时

\[
\left| {f\left( {x, y}\right)  - f\left( {{x}_{0}, y}\right) }\right|  < \frac{\varepsilon }{2}.
\]

要提醒他们,对每一个 \( y, f\left( {x, y}\right) \) 是一个关于 \( x \) 的一元函数,但含有 \( y \) . 因此 \( f\left( {x, y}\right) \) 是一族含参数 \( y \) 的关于 \( x \) 的一元函数,所以上述 \( \delta \) 应该与 \( y \) 有关. 这还不是我们在二元连续时需要的 \( \delta \) .

可以要求学生总结一下: 如果一个二元函数对每个变元分别连续, 还须加何种条件才可保证二元函数连续 (结合例题 18.2.2). 这个问题可以作为习题课上进一步深入的一个内容.
