# 设计说明

这套错题库按四个原则设计。

## 一题一页

每道错题都是独立 Markdown，便于链接、检索、移动、复盘和统计。不要把多题合成一页。

## 字段稳定

YAML 字段用于机器读取，正文用于人看。agent 统计时优先读 YAML，不从自然语言里硬猜。

## 复习优先

错题库不是仓库，而是复习系统。`status`、`mastery`、`repeat_count`、`next_review` 是核心字段。

## 可降级

Dataview、CSS snippet、HTML 看板都是增强体验；即使插件不可用，Markdown 文件本身仍然完整可读。

