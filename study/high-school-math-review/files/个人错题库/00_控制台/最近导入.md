# 最近导入

```dataview
TABLE wrong_id AS 编号, source AS 来源, module AS 专题, topic AS 小节, status AS 状态, created AS 创建, updated AS 更新
FROM "01_错题"
WHERE type = "wrong_problem"
SORT updated DESC, created DESC, file.mtime DESC
LIMIT 50
```

