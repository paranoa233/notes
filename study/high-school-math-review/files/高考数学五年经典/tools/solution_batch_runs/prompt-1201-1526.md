你现在在本地 Obsidian 题库目录中工作：

C:\Users\lucky12345\Documents\高中数学复习\高考数学五年经典

目标：批量为《高考数学五年经典》题库生成每道题的独立解答 Markdown 文件。

这不是计划任务，请直接执行文件读写；不要询问用户确认；不要停在“我将会”。你可以读写本 vault 内文件。

本批次参数：
START_INDEX = 1201
END_INDEX = 1526

严格要求：

1. 只处理本批次范围内的题目。
   - 从 `04_原始资料/problems.json` 读取题目清单。
   - 按 `problems` 数组顺序处理。
   - START_INDEX 和 END_INDEX 使用 1-based 计数。
   - 不处理本范围外题目。

2. 每道题单独生成一个 Markdown 解答文件。
   - 文件路径使用该题在 problems.json 中的 `solution_note` 字段。
   - 也就是写到 `02_我的解答/.../..._我的解答.md`。
   - 不要把多道题合并到一个文件。
   - 解答文件已经真正完成时才跳过：必须同时存在 `## 完整题目`、`## 思路分析`、`## 详细解答`、`## 答案`、`## 总结`，并且 `## 详细解答` 下有实质推导内容。
   - 如果只是模板、空解答、只有 frontmatter、只有“对应题目/## 解答”，必须重新写完整。

3. 每个解答 Markdown 必须包含完整题目。
   - 从对应 `note_path` 读取原题。
   - 提取 `## 题目` 到 `## 我的解答` 之间的内容作为题目主体。
   - 保留题号、题干、选项、图片引用、表格、公式。
   - 如果发现 OCR 明显错误，不要直接改原题文件，而是在解答文件中写“题目勘误”。
   - 若无明显 OCR 问题，写“无明显勘误。”

4. 每个解答文件统一使用以下结构：

---
type: "solution"
problem_id: "题号"
status: "done"
tags:
  - "我的解答"
  - "高考数学五年经典"
---

# 题号 我的解答

对应题目：[[相对路径|题号]]

## 完整题目

粘贴完整题目内容。

## 题目勘误

无明显勘误。

如果有 OCR 问题，写成：
- 原文：...
- 建议更正：...
- 理由：...

## 思路分析

写清楚本题考查点、关键转化、突破口。

## 详细解答

完整书写解题步骤。公式用 Obsidian/Markdown 可识别的 LaTeX：行内用 `\( ... \)`，独立公式用 `\[ ... \]`。

## 答案

明确给出最终答案。选择题写选项，填空题写结果，解答题写结论。

## 总结

总结本题的方法、易错点、可迁移套路。

5. 解答质量要求：
   - 不能只写答案。
   - 选择题、填空题也要给出推理过程。
   - 多选题要逐项判断。
   - 解答题要按步骤完整推导。
   - 如题目依赖图片，应结合图片信息解读；如果仅凭图片引用无法看清细节，说明“图片信息需人工核对”，但仍尽量依据文字完成。
   - 如果 OCR 严重缺失导致无法确定题意，请仍生成解答文件，但 frontmatter 中 `status` 写 `"review"`，并在“题目勘误”中说明缺失点和需要人工核对的位置。
   - 数学推导要严谨，避免跳步过大。
   - 不要使用网络。

6. 完成每题后：
   - 将对应原题 Markdown 的 frontmatter 中 `status` 更新为 `"done"`；如果本题需要人工核对，则更新为 `"review"`。
   - `solution` 字段保持为或更新为对应解答链接。
   - 不要破坏原题正文。
   - 不要删除任何文件。

7. 执行方式：
   - 直接开始处理。
   - 每完成约 20 题，可以简单记录一次进度到输出。
   - 如果遇到单题很难，不要卡死；生成 review 状态说明原因，然后继续下一题。
   - 本批次结束后，统计：本批次总数、已完成、review、跳过、失败文件。

现在开始执行本批次。

